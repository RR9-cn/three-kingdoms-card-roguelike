import {rarityRates,type ForgeState,type Score} from '@three-card/core';
import {COMPANION_IDS,companionRarity,forgeStage} from '@three-card/content';
const fmt=(n:number)=>n.toLocaleString('zh-CN');
export function rarityPanel(s:ForgeState,preview?:Score|null){const target=forgeStage(s).target,pool=COMPANION_IDS.filter(id=>!s.companions.some(c=>c.id===id)),rates=rarityRates(s.stageBest,target,pool),next=s.stageBest<target*.5?Math.ceil(target*.5):s.stageBest<target?target:null;return `<div class="rarity-panel"><b>本关单手最高 ${fmt(s.stageBest)}</b><span>下次征募／刷新首张：良将 ${(rates.common*100).toFixed(1)}% · 名将 ${(rates.rare*100).toFixed(1)}% · 传奇 ${(rates.legendary*100).toFixed(1)}%</span><small>${next?`本手达到 ${fmt(next)} 可提升抽取品质${preview&&preview.total>=next?' · 保底已达门槛':''}`:'已达到最高品质档'}。排除已持有及本批已抽出；某档抽空时按剩余档权重重分配。候选品质在抽出时锁定。</small></div>`;}
export function settlementPanel(s:ForgeState,ready:boolean){if(!s.result)return'';if(!ready||s.settlement==='pending')return '<p class="reveal-prompt">连锁揭晓中 · 最终攻势将在揭晓后直接入账</p>';const remaining=Math.max(0,s.target-s.score);return `<div class="settlement-result"><b>本手攻势已入账 ${fmt(s.result.total)}</b><span>${remaining?`本关还差 ${fmt(remaining)} 攻势`:'已达到本关目标'}</span>${s.result.total>s.recordBefore?`<strong class="new-record">刷新本局纪录！${fmt(s.recordBefore)} → ${fmt(s.result.total)}</strong>`:''}</div>`;}
