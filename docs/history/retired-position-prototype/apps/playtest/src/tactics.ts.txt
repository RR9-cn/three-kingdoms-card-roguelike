import {newTactics,tacticalAction,GENERALS,enemyIntent,type TacticalState,type TacticalAction,type GeneralId} from '@three-card/core';
const root=document.querySelector<HTMLDivElement>('#app')!;let s=newTactics(),selected:GeneralId='zhangfei',undo:TacticalState[]=[],notice='',paused=false,help=false,hints=false;
const names=['先锋','中军','后援'];
const btn=(label:string,action:string,disabled=false,cls='')=>`<button data-action="${action}" ${disabled?'disabled':''} class="${cls}">${label}</button>`;
function render(){const index=s.allies.findIndex(u=>u.id===selected),unit=s.allies[index],ended=['victory','defeat'].includes(s.phase);root.innerHTML=`<header><div><span>三张为阵</span> · 破阵试验场 <small>动作验证小样</small></div><nav><a href="./index.html">返回构筑版</a>${btn('规则','help')}${btn('重新摆阵','reset')}</nav></header><div class="layout"><main><div class="title"><div><p class="eyebrow">黄巾水寨 · 三回合内消灭敌军，所有武将存活</p><h1>${s.phase==='deploy'?'先排位，再破阵':s.phase==='victory'?'此阵已破':s.phase==='defeat'?'此路未通':'先破盾线，还是先除弓手？'}</h1></div><div class="resource">第 <b>${s.round}</b> / 3 回合<br>军令 <b>${s.commands}</b> / 4</div></div><div class="enemy-board">${s.enemies.map((e,i)=>`<div class="enemy-slot ${e?'occupied':'gap'}"><small>敌${names[i]}</small>${e?`<span class="portrait">${e.id==='shield'?'盾':'弓'}</span><h2>${e.id==='shield'?'盾兵':'弓手'}</h2><p>${e.hp} 兵力</p><span>${e.id==='shield'?(i===0?'阻挡通道 · 攻击我方先锋':'盾线后撤 · 无法阻挡突袭'):'后排齐射 · 攻击我方后援'}</span>`:'<b>空位</b><span>赵云可穿过先锋缺口突袭后排</span>'}</div>`).join('')}</div><div class="intent"><b>结束回合后，敌军将：</b>${enemyIntent(s).map(t=>`<span>${t}</span>`).join('')||'<span>敌军已被消灭</span>'}</div><div class="ally-board">${s.allies.map((u,i)=>`<article class="general ${u.id===selected?'selected':''}"><div class="position">${names[i]} <small>${i+1}</small></div><button class="select-general" data-action="select:${u.id}"><span class="portrait">${GENERALS[u.id].name[0]}</span><h2>${GENERALS[u.id].name}</h2><p>${u.hp} / ${GENERALS[u.id].hp} 兵力 · ${u.acted?'已行动':'待命'}</p></button><div class="movement">${btn('←',`swap:${i}:${i-1}`,i===0||ended,'arrow')}${btn('→',`swap:${i}:${i+1}`,i===2||ended,'arrow')}</div></article>`).join('')}</div><div class="action-box"><div><h2>${GENERALS[selected].name} · ${GENERALS[selected].skill}</h2><p>${GENERALS[selected].text}</p><small>${s.phase==='deploy'?'战前箭头换位免费。默认中军诸葛亮可支援两侧。':'战中换位花1军令，已行动状态跟随武将。普通攻击和技能各花1军令。'}</small></div><div class="commands">${selected==='zhugeliang'?s.allies.filter((u,i)=>Math.abs(i-index)===1).map(u=>btn(`再令${GENERALS[u.id].name}`,`skill:${u.id}`,s.phase!=='player'||unit.acted||!u.acted||s.commands<1,'primary')).join(''):btn(GENERALS[selected].skill,'skill',s.phase!=='player'||unit.acted||s.commands<1||(selected==='zhangfei'&&index!==0),'primary')}${btn('普通攻击 · 2伤害','attack',s.phase!=='player'||unit.acted||s.commands<1)}</div></div><footer>${s.phase==='deploy'?btn('开始破阵','begin',false,'primary'):!ended?btn('结束回合 · 承受已显示的攻击','end',false,'danger'):btn('重摆，再试一种解法','reset',false,'primary')}${btn('撤回一步','undo',undo.length===0)}<span>${notice||'没有隐藏伤害和随机判定。试着改变位置与出手顺序。'}</span></footer></main><aside><p class="eyebrow">战场演变</p><div class="log" aria-live="polite">${s.log.map((l,i)=>`<p class="${i===s.log.length-1?'latest':''}">${l}</p>`).reverse().join('')}</div><div class="hint">${btn(hints?'收起思路':'需要一点思路？','hints')}${hints?`<h3>试两种思路</h3><p>推开盾兵 → 赵云突袭 → 再令赵云。</p><p>推开盾兵 → 再令张飞 → 制造碰撞。</p><small>同样的敌阵，比较谁被击杀、谁承伤，以及还剩多少军令。</small>`:'<p>先自由试摆。每个动作都能撤回。</p>'}</div></aside></div>${paused||help?`<div class="overlay"><section><h2>${paused?'战场暂停':'动作如何相互配合'}</h2><p>每位武将每回合先行动一次。4军令用于攻击、技能或战中换位。诸葛亮再令只恢复友将行动资格，友将再次出手仍需1军令。</p><p>张飞击退会打开通道；通道空出后赵云能穿过前排。赵云自己也会换到先锋，改变诸葛亮的相邻目标与敌军攻击对象。</p><p>点击武将选中，箭头换位；1–3选将。试验场允许撤回，包括敌军回合，鼓励比较解法。当前小样不存档，重开应用会重置。</p>${btn('继续','close',false,'primary')}</section></div>`:''}`;}
function handle(action:string){const[id,a,b]=action.split(':');if(id==='close'){help=false;paused=false;render();return;}if(paused)return;if(id==='help'){help=true;render();return;}if(id==='hints'){hints=!hints;render();return;}if(id==='reset'){s=newTactics();undo=[];notice='';selected='zhangfei';render();return;}if(id==='select'){selected=a as GeneralId;render();return;}if(id==='undo'){const prior=undo.pop();if(prior)s=prior;notice='已撤回，可尝试另一种行动。';render();return;}const command:Omit<TacticalAction,'seq'>=id==='swap'?{type:'swap',from:Number(a),to:Number(b)}:id==='skill'?{type:'skill',actor:selected,target:a as GeneralId}:id==='attack'?{type:'attack',actor:selected}:{type:id as 'begin'|'end'};const r=tacticalAction(s,{...command,seq:s.seq});if(r.error)notice=r.error;else{undo.push(s);s=r.state;notice='';}render();}
root.addEventListener('click',e=>{const el=(e.target as HTMLElement).closest<HTMLButtonElement>('[data-action]');if(el&&!el.disabled)handle(el.dataset.action!);});window.addEventListener('keydown',e=>{if(e.repeat||help||paused)return;if(/^[1-3]$/.test(e.key))handle('select:'+s.allies[Number(e.key)-1].id);});function pause(){if(s.phase==='player'){paused=true;render();}}window.addEventListener('blur',pause);(window as Window&{desktopLifecycle?:{onPause:(f:()=>void)=>void}}).desktopLifecycle?.onPause(pause);Object.assign(window,{render_tactics_to_text:()=>JSON.stringify(s)});render();
