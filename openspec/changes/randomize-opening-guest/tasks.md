## 1. Opening flow

- [x] 1.1 Generate one seeded opening guest from the active pool with existing rarity weights.
- [x] 1.2 Replace the three-choice opening copy and UI expectations with a single revealed guest confirmation.

## 2. AI play

- [x] 2.1 Make `starter` optional for public random starts while preserving explicit active-guest fixtures.
- [x] 2.2 Update the AI play skill and API documentation for the random default.

## 3. Verification

- [x] 3.1 Cover deterministic random openings, active-pool eligibility, legacy explicit starts, browser flow, build, and strict OpenSpec validation.
