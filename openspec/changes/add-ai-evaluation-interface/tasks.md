## 1. Evaluation tuning

- [x] 1.1 Add optional transient core tuning for stage targets and Liu Bei multiplier
- [x] 1.2 Pass tuning through built-in public-view strategies without changing default behavior

## 2. AI evaluation operations

- [x] 2.1 Implement deterministic run tracing and compact `simulate_batch` aggregation
- [x] 2.2 Implement bounded `inspect_run` stage summaries
- [x] 2.3 Implement paired `compare_rules` variants and delta ranking
- [x] 2.4 Add the versioned JSON CLI with file and stdin input

## 3. Contract and verification

- [x] 3.1 Add validation, determinism, parity, output-bound, and comparison tests
- [x] 3.2 Document request schemas, limits, examples, and interpretation boundaries
- [x] 3.3 Run full tests, sample all operations, validate output size, and strictly validate the OpenSpec change
